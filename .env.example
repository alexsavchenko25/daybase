# Supabase Cloud Sync (optional) — kopiere nach .env und fülle aus.
# Ohne diese Werte läuft Daybase weiter rein lokal (IndexedDB), kein Sync.
# Werte findest du im Supabase-Dashboard unter Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
